# Run 001 - 2026-06-19

## Setup

- Build/install: `dotnet build` succeeded with 0 warnings and 0 errors.
- Launch: direct `SlayTheSpire2.exe` launch succeeded after the Steam app shortcut did not produce a window.
- Run: unseeded couch co-op, 4 player slots.
- Characters: Ironclad, Silent, Defect, Necrobinder.

## Result

Game over on Act 1, floor 2, first monster after Neow/Ancient.

Final API state was `state_type: overlay` with `overlay.screen_type: NGameOverScreen`. STS2 was still responsive and both local endpoints were healthy, so this was treated as a completed run rather than a crash.

## Flow

- Ironclad: Chose Small Capsule from Neow/Ancient, then claimed the generated Ornamental Fan relic reward. Entered floor 2 combat against Shrinker Beetle and died.
- Silent: Chose Lost Coffer from Neow/Ancient. No follow-up private Lost Coffer card/potion reward was observed before the shared reward flow proceeded. Died in the floor 2 combat.
- Defect: Chose Neow's Torment from Neow/Ancient. Died in the floor 2 combat.
- Necrobinder: Chose Lost Coffer from Neow/Ancient. Osty appeared in shared combat state. No follow-up private Lost Coffer card/potion reward was observed before the shared reward flow proceeded. Died in the floor 2 combat.

## Issues And Quirks

- First `start_couch` attempt failed with `Wait until the main menu is fully loaded before starting couch co-op`; retrying after a short wait succeeded.
- Map voting on the first post-Ancient branch reported `Final couch map vote received` on the third of four submitted votes, then the fourth vote still returned success afterward.
- Slot 0's Ancient option list did not represent the option titles later chosen by the other slots. This may be valid private per-player RNG, but it makes shared inspection misleading unless each slot is queried separately.
- After private Ancient picks, the run entered a shared `rewards` screen showing Ironclad's Small Capsule follow-up relic, Ornamental Fan, for every slot. Silent and Necrobinder both had Lost Coffer relics, but no private card reward or potion procurement was observed.
- Game over is exposed only as generic `state_type: overlay` / `NGameOverScreen`, with a message saying manual in-game interaction may be required. The phone/API flow needs a way to return to menu or reset from this state.

## Notes

The combat driver used a very naive survival strategy and may have played poorly. The important mod-facing result is not the loss itself, but that the run reached game over through the API and then halted at a generic overlay state.
