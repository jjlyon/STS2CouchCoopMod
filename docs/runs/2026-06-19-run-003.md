# Run 003 - 2026-06-19

## Setup

- Closed STS2, ran `dotnet build`, and relaunched `SlayTheSpire2.exe`.
- Build/install succeeded with 0 warnings and 0 errors.
- Run: unseeded couch co-op, 4 player slots.
- Characters: Ironclad, Silent, Defect, Necrobinder.

## Result

Game over on Act 1, floor 2, first monster after Neow/Ancient.

Unlike Run 001, the final API state was `state_type: game_over` rather than a generic `overlay` / `NGameOverScreen`. This looks like an improvement in the latest build.

## Flow

- Ironclad: Chose Scroll Boxes, selected bundle 0 containing Pommel Strike, Body Slam, and Howl from Beyond, then proceeded to map.
- Silent: Chose Fishing Rod.
- Defect: Chose Arcane Scroll.
- Necrobinder: Chose Large Capsule, receiving Ornamental Fan and Permafrost.

The party selected the first monster branch that led toward an early shop. Combat entered correctly against Nibbit with a full `battle` payload and private hands for all slots. This did not reproduce the Run 002 `Combat ended. Waiting for rewards...` blocker.

The party died during the Nibbit combat. The combat was played with the simple test strategy, and the loss appears gameplay-related rather than a crash.

## Issues And Quirks

- First `start_couch` attempt after fresh launch still failed with `Wait until the main menu is fully loaded before starting couch co-op`; retrying after a short wait succeeded.
- After all Neow choices completed, top-level per-slot `event.options` was empty even though `shared.event.options` contained a `Proceed` option. `choose_event_option` index 0 failed with `Event option index 0 out of range (0 options)`, but generic `proceed` succeeded.
- The previous Run 002 first-combat blocker appears improved: first combat now populated enemy and hand state correctly after a short delay.
- The previous Run 001 game-over overlay issue appears improved: final state was `game_over`.

## Notes

The Nibbit fight had a very large HP pool and hit all players for 12 on turn 1. The current naive strategy is enough to exercise action plumbing but not enough to reliably survive early high-pressure encounters.
