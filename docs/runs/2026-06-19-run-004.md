# Run 004 - 2026-06-19

## Setup

- Started from the main menu after resetting Run 003.
- Run: unseeded couch co-op, 4 player slots.
- Characters: Ironclad, Silent, Defect, Necrobinder.

## Result

Unplayable roadblock on Act 1, floor 2, immediately after choosing the first monster node.

The API reported `state_type: monster` with message `Combat ended. Waiting for rewards...`, but there was no battle payload, no rewards screen, and no enabled proceed action. This reproduces the Run 002 blocker in the latest build.

## Flow

- Ironclad: Chose Precise Scissors, entered `card_select`, selected a Strike, and confirmed selection successfully.
- Silent: Chose Fishing Rod.
- Defect: Chose Fishing Rod.
- Necrobinder: Chose Cursed Pearl and gained 333 gold plus Greed.

After Ironclad's card removal, the run returned to the Neow done event. Generic `proceed` returned to map. The party chose the branch at `(3,1)`, then immediately hit the stuck `Combat ended. Waiting for rewards...` state.

## Issues And Quirks

- Card removal worked with `select_card` followed by `confirm_selection`.
- During Ironclad's `card_select`, slot 0 reported top-level `state_type: card_select`, while other slots reported top-level `state_type: event` but also included the shared `card_select` data. This mixed per-slot/shared state is confusing for clients.
- The top-level Neow event still had `options: []` while shared state exposed a Proceed option; generic `proceed` remained necessary.
- The Run 002 first-combat blocker reproduced in the latest build.

## Recovery Tried

- Waited 5 seconds for combat or rewards to appear; state did not change.
- Tried `end_turn`; returned `Not in combat`.
- Tried `proceed`; returned `No proceed button available or enabled`.
- Tried `claim_reward` index 0; returned `Rewards screen is not open`.
