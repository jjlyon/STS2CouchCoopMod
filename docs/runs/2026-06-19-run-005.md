# Run 005 - 2026-06-19

## Setup

- Started from the main menu after resetting Run 004.
- Run: unseeded couch co-op, 4 player slots.
- Characters: Ironclad, Silent, Defect, Necrobinder.

## Result

Unplayable roadblock on Act 1, floor 2, immediately after choosing the first monster node.

The API reported `state_type: monster` with message `Combat ended. Waiting for rewards...`, but there was no usable battle payload, no rewards screen, and no enabled proceed action.

## Flow

- Ironclad: Chose Lead Paperweight, then completed a `card_select` prompt by choosing Dark Shackles.
- Silent: Chose New Leaf, then completed a transform prompt by selecting Strike.
- Defect: Chose Pomander, then completed an upgrade prompt by selecting Strike.
- Necrobinder: Chose Massive Scroll, then completed a choose-card prompt by selecting Lift.

Important testing correction: all four players' Neow follow-up prompts were cleared before proceeding to the map. Only after all slots reported no pending `card_select` did slot 0 use generic `proceed` to leave Neow.

The party then selected the monster at `(3,1)` because it led to an Unknown node. The run immediately entered the stuck `Combat ended. Waiting for rewards...` state.

## Issues And Quirks

- The first-combat blocker reproduced even after all four players completed their Neow follow-up prompts. This suggests the blocker is not merely caused by skipped per-player prompt/reward acceptance.
- During Neow cleanup, top-level `event.options` remained empty while `shared.event.options` contained Proceed; `choose_event_option` index 0 failed and generic `proceed` was still required.
- Some choose-card prompts select immediately and make `confirm_selection` return `No card selection screen is open`; this is expected for those prompt types, but clients need to tolerate it.

## Recovery Tried

- Waited 5 seconds for combat or rewards to appear; state did not change.
- Tried `end_turn`; returned `Not in combat`.
- Tried `proceed`; returned `No proceed button available or enabled`.
- Tried `claim_reward` index 0; returned `Rewards screen is not open`.
