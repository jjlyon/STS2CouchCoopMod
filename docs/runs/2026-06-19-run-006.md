# Run 006 - 2026-06-19

## Setup

- Started from the main menu after resetting Run 005.
- Run: unseeded couch co-op, 4 player slots.
- Characters: Ironclad, Silent, Defect, Necrobinder.

## Result

Unplayable roadblock on Act 1, floor 2, immediately after all four players voted into the first monster node.

The API reported `state_type: monster` with message `Combat ended. Waiting for rewards...`, but there was no usable battle payload, no rewards screen, and no enabled proceed action.

## Flow

- Ironclad: Chose Golden Pearl and gained gold.
- Silent: Chose Lead Paperweight, then completed a choose-card prompt by selecting Fisticuffs.
- Defect: Chose Fishing Rod.
- Necrobinder: Chose Fishing Rod.

Important testing correction: before traveling to combat, all four slots were explicitly verified on `state_type: map`. Then all four slots submitted `choose_map_node` for the same monster at `(0,1)`, and all four vote actions returned success. The fourth vote returned `Final couch map vote received; traveling to Monster at (0,1)`.

Immediately after travel, slot 0 reported `state_type: monster` with `Combat ended. Waiting for rewards...`.

## Issues And Quirks

- The first-combat blocker reproduced even when all four players were confirmed on the map before combat and all four submitted successful map votes. This suggests the blocker is not caused by only one player proceeding/voting to combat.
- The Neow done event still exposed `event.can_proceed: true` with top-level `event.options: []`; generic `proceed` was required to return to map.

## Recovery Tried

Not repeated in this run because the state matched the already documented blocker exactly. Previous recovery attempts on this state returned:

- `end_turn`: `Not in combat`
- `proceed`: `No proceed button available or enabled`
- `claim_reward`: `Rewards screen is not open`
