# Run 010 - 2026-06-20

## Setup

- STS2 was launched without rebuilding; latest installed mod was used.
- Run: unseeded couch co-op, 4 player slots.
- Characters: Ironclad, Silent, Defect, Necrobinder.

## Result

Game over on Act 1, floor 2, during the first monster combat against Nibbit.

This run did not reproduce the first-combat `Combat ended. Waiting for rewards...` blocker. The first monster had a real battle payload, all slots had actionable hands, and the run ended cleanly with `state_type: game_over`.

## Flow

- Ironclad: Chose Fishing Rod.
- Silent: Chose Scroll Boxes, selected and confirmed a bundle, and did not receive a second Neow reward afterward.
- Defect: Chose Lead Paperweight, selected Flash of Steel, and did not receive a second Neow reward afterward.
- Necrobinder: Chose Nutritious Oyster, raising max HP to 77.

After all follow-up prompts resolved, all four slots showed Neow done/proceed state. Generic `proceed` returned the party to map. All four slots were verified on map, then all four voted into the monster at `(0,1)`.

Combat entered correctly against Nibbit and remained actionable until the party died.

## Issues And Quirks

- Top-level Neow `event.options` was still empty after completion while `shared.event.options` contained Proceed, so generic `proceed` was still required.
- The test driver can still send stale card indexes after hands shift; the API handled these as errors without crashing.
- The Nibbit fight remains too lethal for the naive strategy, but this appears gameplay-related rather than a mod-state failure.

## Fix Validation Notes

- Multiple Neow reward grab did not reproduce after Scroll Boxes.
- First-combat limbo did not reproduce.
- Game over state was clean.
