# Run 014 - 2026-06-20

## Setup

- STS2 was already running with the latest installed mod.
- Run: unseeded couch co-op, 4 player slots.
- Characters: Ironclad, Silent, Defect, Necrobinder.

## Result

Unplayable roadblock on Act 1, floor 2, immediately after all four slots voted into the first monster node.

All four slots reported `state_type: monster` with `Combat ended. Waiting for rewards...`, but no battle, rewards, event, card-selection, card-reward, or map state was available. The players were all alive and no combat payload was exposed.

## Flow

The run began with an Ancient node at floor 0, which opened Neow.

- Ironclad: Chose Arcane Scroll and returned to Neow done state with `can_proceed: true`.
- Silent: Chose Stone Humidifier and returned to Neow done state with `can_proceed: true`.
- Defect: Chose Golden Pearl, gained gold from 99 to 249, and returned to Neow done state with `can_proceed: true`.
- Necrobinder: Chose New Leaf, selected Strike for transform, and returned to Neow done state with `can_proceed: true`.

After all four slots proceeded, all slots were verified on map with the expected relics and no second Neow reward offer. The party chose the first monster at `(3,1)`, which led to an Unknown node and was one of the better survivability paths.

All four slots voted successfully, and the final response queued travel to the monster. The state then landed in the combat-ended limbo blocker instead of a live battle.

## Issues And Quirks

- Reproduced the first-combat limbo blocker again after a clean Neow sequence and all four map votes.
- Startup still emitted transient `state=unknown` updates before the first map state.
- Later `proceed` responses after slot 0 were blank, but all slots still reached map successfully.

## Fix Validation Notes

- Multiple Neow reward grab did not reproduce.
- No cross-slot prompt leakage or canonical model state-read error reproduced in this run.
- First-combat limbo did reproduce.
